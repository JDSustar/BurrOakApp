<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity$PlaceholderFragment">


    <Button
        android:text="Hiking Trails"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/hikingButton"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:contentDescription="Hiking"
        android:onClick="viewHikingInformation"
        android:minHeight="50dp"
        android:minWidth="50dp"/>

    <Button
        android:text="Marina Information"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/marinaButton"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/hikingButton"
        android:contentDescription="Marina"
        android:onClick="viewMarinaInformation"/>

    <Button
        android:text="Events"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/eventsButton"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/marinaButton"
        android:contentDescription="Events"
        android:onClick="viewEventInformation"/>

</LinearLayout>
